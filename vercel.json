{
  "buildCommand": "npx ng build --configuration production",
  "installCommand": "npm install",
  "outputDirectory": "dist/jamm-angular/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
